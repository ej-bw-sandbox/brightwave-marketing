---
export async function getStaticPaths(){ return [{ params: { slug: 'private-markets' } }] }
const slug = Astro.params.slug!
if (slug !== 'private-markets') Astro.response.status = 404
return Astro.redirect(slug === 'private-markets' ? '/private-markets-platform' : '/', 301)
---
